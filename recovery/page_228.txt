"import { useState, use } from \"react\";\nimport Link from \"next/link\";\nimport styles from \"./page.module.css\";\n\nexport default function ModuleOne({ params }: { params: Promise<{ id: string }> }) {\n  const unwrappedParams = use(params);\n  const [taskIndex, setTaskIndex] = useState(0);\n  const [windowOpen, setWindowOpen] = useState(false);\n  const [shortcutPressed, setShortcutPressed] = useState(false);\n\n  const tasks = [\n    { title: \"Task 1: Open the Settings App\", instruction: \"Double click the settings icon on the desktop to open it.\" },\n    { title: \"Task 2: Minimize the Window\", instruction: \"Click the minimize button (the dash) on the settings window.\" },\n    { title: \"Task 3: Keyboard Shortcuts\", instruction: \"Press 'Ctrl + C' to copy the secret code.\" },\n  ];\n\n  const logEvent = async (action: string) => {\n    try {\n      await fetch('/api/events', {\n        method: 'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body: JSON.stringify({ moduleId: unwrappedParams.id, actionType: action, metadataJson: {} })\n      });\n    } catch (e) {\n      console.error(e);\n    }\n  };"
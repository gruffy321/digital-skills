"\"use client\";\n\nimport { useState } from \"react\";\nimport Link from \"next/link\";\nimport styles from \"./page.module.css\";\n\nexport default function ModuleOne({ params }: { params: { id: string } }) {\n  const [taskIndex, setTaskIndex] = useState(0);\n  const [windowOpen, setWindowOpen] = useState(false);\n  const [shortcutPressed, setShortcutPressed] = useState(false);\n\n  const tasks = [\n    { title: \"Task 1: Open the Settings App\", instruction: \"Double click the settings icon on the desktop to open it.\" },\n    { title: \"Task 2: Minimize the Window\", instruction: \"Click the minimize button (the dash) on the settings window.\" },\n    { title: \"Task 3: Keyboard Shortcuts\", instruction: \"Press 'Ctrl + C' to copy the secret code.\" },\n  ];\n\n  const handleDoubleClick = () => {\n    if (taskIndex === 0) {\n      setWindowOpen(true);\n      setTimeout(() => setTaskIndex(1), 1000);\n    }\n  };\n\n  const handleMinimize = () => {\n    if (taskIndex === 1) {\n      setWindowOpen(false);\n      setTimeout(() => setTaskIndex(2), 1000);\n    }\n  };\n\n  const handleKeyDown = (e: React.KeyboardEvent) => {\n    if (taskIndex === 2 && e.ctrlKey && e.key === 'c') {\n      setShortcutPressed(true);\n      setTimeout(() => setTaskIndex(3), 1000);\n    }\n  };\n\n  return (\n    <div className={styles.container} onKeyDown={handleKeyDown} tabIndex={0}>\n      <header className={`glass-panel ${styles.topBar}`}>\n        <Link href=\"/dashboard\" className={styles.backBtn}>← Back to Dashboard</Link>\n        <div className={styles.taskPanel}>\n          {taskIndex < tasks.length ? (\n            <>\n              <h3>{tasks[taskIndex].title}</h3>\n              <p>{tasks[taskIndex].instruction}</p>\n            </>\n          ) : (\n            <div className={styles.successMessage}>\n              <h3>🎉 Module Complete!</h3>\n              <p>You have successfully completed the Introduction to Digital Skills.</p>\n            </div>\n          )}\n        </div>\n      </header>\n\n      <main class
<truncated 1470 bytes>